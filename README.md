# yoomoney

```shell
# copies .env.example to .env
make config

# build + run
make

# builds containers
make build

# runs containers
make run

# shutdowns the application
make down

# healthcheck
make ps

# check logs, usage 'make logs AT=app'
make logs
```